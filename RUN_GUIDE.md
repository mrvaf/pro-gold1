# راهنمای جامع راه‌اندازی و اجرای پلتفرم V-GOLD

این راهنما مراحل کامل دانلود، نصب پیش‌نیازها، پیکربندی، تست و اجرای پلتفرم دیجیتال طلا و جواهر **V-GOLD** را توضیح می‌دهد.

---

## ۱. نحوه دانلود و دریافت کد پروژه

پروژه به صورت کامل روی گیت‌هاب قرار دارد و تمامی ۲۶ مرحله در برنچ `arena/01a0d8b1-pro-gold1` پوش شده است.

### روش ۱: کلون با Git (پیشنهادی)

```bash
# کلون کردن ریپازیتوری
git clone -b arena/01a0d8b1-pro-gold1 https://github.com/mrvaf/pro-gold1.git

# ورود به پوشه پروژه
cd pro-gold1
```

### روش ۲: دانلود فایل فشرده (ZIP) مستقیم از گیت‌هاب
می‌توانید به آدرس زیر مراجعه کرده و فایل ZIP این برنچ را دانلود نمایید:
`https://github.com/mrvaf/pro-gold1/tree/arena/01a0d8b1-pro-gold1`
(با کلیک روی دکمه سبز رنگ Code و انتخاب Download ZIP).

---

## ۲. پیش‌نیازهای سیستمی

قبل از اجرا اطمینان حاصل کنید نرم‌افزارهای زیر روی سیستم شما نصب هستند:
* **Node.js**: نسخه `20.x` یا `22.x` (LTS پیشنهاد می‌شود).
* **npm**: نسخه `10.x` یا بالاتر (همراه با Node.js نصب می‌شود).
* **Git**: جهت دریافت کدهای به‌روز.
* **PostgreSQL** (اختیاری): پلتفرم به صورت پیش‌فرض با مخازن درون‌حافظه‌ای ایزوله (In-Memory) کار می‌کند و نیازی به نصب دیتابیس جداگانه برای تست یا اجرای آزمایشی ندارد. در صورت نیاز به دیتابیس واقعی، نسخه PostgreSQL 15 یا بالاتر پیشنهاد می‌شود.

---

## ۳. نصب وابستگی‌ها (Dependencies)

به دلیل ساختار Monorepo (چندپکیجی) با npm Workspaces، تمامی پکیج‌ها با یک دستور از ریشه پروژه نصب می‌شوند:

```bash
npm install
```

---

## ۴. بررسی کیفیت و تست‌ها (Verification)

تمامی ۱۰۶ سوئیت آزمون و ۶۷۲ تست واحد و یکپارچگی را می‌توانید اجرا کنید:

```bash
# اجرای تست‌های واحد، مالی، امنیتی و یکپارچگی
npm test

# بررسی تایپ‌ها و اعتبار کدهای تایپ‌اسکریپت در تمام پکیج‌ها
npm run typecheck
```

---

## ۵. بیلد و کامپایل پروژه (Build)

برای کامپایل پکیج‌های دامنه، هوش مصنوعی، دیتابیس و اپلیکیشن وب Next.js:

```bash
npm run build
```

---

## ۶. اجرای پلتفرم (Run Development / Production)

### اجرای محیط توسعه (Development Mode)

```bash
# اجرای سرور توسعه Next.js
npm run dev --workspace=@v-gold/web
```
اپلیکیشن در آدرس زیر در دسترس خواهد بود:
👉 **`http://localhost:3000`**

### اجرای نسخه پروداکشن (Production Server)

پس از اجرای `npm run build`:

```bash
npm run start --workspace=@v-gold/web
```

---

## ۷. بررسی سلامت سیستم و اندپوینت‌ها (Health Probes)

پس از بالا آمدن سرور، می‌توانید وضعیت سلامت و لایونس کانتینرها را از طریق اندپوینت‌های زیر بررسی کنید:

* **بررسی زنده بودن پروسس (Liveness):**
  ```bash
  curl http://localhost:3000/api/health/live
  ```
  پاسخ مورد انتظار: `{"status":"healthy","probe":"liveness", ...}`

* **بررسی آمادگی ذخیره‌سازی و سرویس‌ها (Readiness):**
  ```bash
  curl http://localhost:3000/api/health/ready
  ```
  پاسخ مورد انتظار: `{"status":"ready","probe":"readiness","storage":{"mode":"in-memory"}, ...}`

---

## ۸. ساختار پکیج‌ها در پروژه

```text
├── apps/
│   └── web/                   # وب‌اپلیکیشن و ۴۵ روت Next.js 15
├── packages/
│   ├── core/                  # دامنه خالص، ارزش‌گذاری مالی، محاسبات طلا و قوانین تجاری
│   ├── database/              # ۲۵ مایگریشن SQL ترتیبی، Drizzle ORM و آداپتورها
│   └── ai-gateway/            # گیت‌وی هوش مصنوعی، کانسپت دیزاینر و موتور بصری
├── tests/                     # سوئیت‌های تست Vitest (۶۷۲ تست)
├── ARCHITECTURE.md            # مستند ۶۷ تصمیم معماری (ADR-0001 تا ADR-0067)
├── PROJECT_STATE.md           # کارنامه کنترل کیفیت و وضعیت هر مرحله
└── ROADMAP.md                 # نقشه راه ۲۶ مرحله‌ای
```
